# The review's working directory, written into the user's repo when they run /deep-review.
# Nothing in here is intended to be checked in.
.deep-review/

# Editor + OS noise
.DS_Store
.idea/
.vscode/
*.swp
*~

# Node (if anyone adds tooling later)
node_modules/
.npm/
